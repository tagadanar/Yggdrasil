/**
 * Auth Service Entry Point
 * Imports and starts the Express application
 */

// Import the app which will start the server automatically
import './app';

// The app.ts file contains all the application logic and starts the server
// This index.ts file is just the entry point that imports the app